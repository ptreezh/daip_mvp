{
  "auto-prompt": "你现在是一位遵循业界最高代码质量标准的资深 Python 开发者。从现在开始，你生成的所有 Python 代码都必须严格遵守以下规则：\n\n1.  **【日志记录】**: 所有日志记录（logging）必须使用惰性格式化。例如 `logging.info('消息: %s', my_var)`，严禁使用 f-string 如 `logging.info(f'消息: {my_var}')`。\n2.  **【模块导入】**: 所有 `import` 语句必须置于文件顶部，并严格按照 **标准库、第三方库、本项目库** 的顺序分组，每组内部按字母顺序排序。\n3.  **【未用参数】**: 如果一个函数参数因接口兼容性等原因必须存在但未被使用，必须在其名称前加上下划线。例如 `def callback(self, _event_data):`。\n4.  **【类型提示】**: 所有函数定义（参数和返回值）和类属性都必须有明确的 PEP 484 类型提示。\n5.  **【文档字符串】**: 所有公开的模块、类和函数都必须有符合 **Google 风格** 的文档字符串（Docstring），清晰地说明其功能、参数和返回值。\n\n在遵循以上规则的前提下，开始处理我的请求。",
  "type_check": {
    "enabled": true,
    "tool": "mypy",
    "fail_on_error": true
  },
  "lint": {
    "enabled": true,
    "tool": "pylint",
    "fail_on_error": true
  },
  "docstring_required": true,
  "api_doc_generation": {
    "enabled": true,
    "tool": "sphinx"
  },
  "naming_convention": {
    "function": "snake_case",
    "class": "PascalCase",
    "variable": "snake_case",
    "constant": "UPPER_CASE"
  },
  "mock_signature_check": true
}
