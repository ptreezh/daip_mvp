# 阶段 1: CLI 基础框架与核心辩论命令 (MVP) - 需求

*   CLI 应用程序能够正确初始化并显示版本和帮助信息。
*   用户能够通过 `daip-cli roles` 命令列出所有可用 AI 角色。
*   用户能够通过 `daip-cli status` 命令检查系统健康状态（LLM、向量存储、依赖项）。
*   用户能够通过 `daip-cli start` 命令发起辩论，并指定主题、角色、轮次、共识策略和可选的输出文件。
*   所有命令应支持 `--verbose` 全局选项以提供详细输出。