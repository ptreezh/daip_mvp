# 开发规则和约束

## 核心原则
1. **严格遵守纪律** - 这是最高准则
2. **诚实透明** - 不报告虚假成果
3. **规则先行** - 所有工作必须基于明确的规则和规范

## TDD实施规则

### 1. 测试驱动开发流程
- **红灯**：先编写失败的测试用例
- **绿灯**：编写最少代码使测试通过
- **重构**：优化代码结构，保持测试通过
- **重复**：继续下一个测试用例

### 2. 实施步骤
1. **需求分析**：明确要实现的功能
2. **测试计划**：编写详细的测试计划文档
3. **测试用例**：先编写测试代码
4. **执行测试**：运行测试，确认失败（红灯）
5. **实现功能**：编写实现代码使测试通过（绿灯）
6. **重构优化**：优化代码结构
7. **集成测试**：与系统其他部分集成测试
8. **文档更新**：更新相关文档

### 3. 质量要求
- **测试覆盖率**：必须达到90%以上
- **代码审查**：所有代码必须经过审查
- **静态检查**：通过所有静态代码检查
- **功能验证**：实际运行验证功能正确性

## 文档规范
1. **BMAD方法**：遵循BMAD文档方法
2. **KIRO规范**：遵循KIRO规范
3. **及时更新**：代码变更后立即更新相关文档

## 代码规范
1. **SOLID原则**：遵循SOLID设计原则
2. **KISS原则**：保持简单
3. **YAGNI原则**：不要实现不需要的功能
4. **代码复用**：优先使用现有组件
5. **导入规范 (Import Convention)**: 本项目所有内部模块的导入，都 **必须** 从包名 `daip_live` 开始， **严禁** 使用 `src.` 作为前缀。 (例如: `from daip_live.core import...` 是正确的, `from src.daip_live.core import...` 是错误的)。

## 项目结构规范 (Project Structure Convention)
1. **Src Layout**: 本项目遵循标准的 `src-layout` 结构。
2. **源码路径**: 所有 `daip_live` 包的Python源代码 **必须** 存放在 `src/daip_live/` 目录及其子目录中。
3. **根目录**: 项目根目录 **不应** 包含任何 `daip_live` 包的模块文件（`.py` 文件）。根目录用于放置配置文件、文档、测试等非包代码。
4. **模块创建**: 创建新功能模块时，应在 `src/daip_live/` 下创建新的子目录，并必须包含一个 `__init__.py` 文件以将其定义为Python包。
5. **避免命名冲突 (CRITICAL)**: 在创建新的子目录（包）之前， **必须** 检查父目录中是否存在同名的 `.py` 文件（模块）。文件和目录同名是Python导入系统中的严重冲突，必须避免。

## 禁止行为
1. **虚假报告**：不得报告未验证的功能
2. **跳过步骤**：不得跳过任何规定的步骤
3. **自行其是**：不得在没有明确指令的情况下自行决定实现方式
4. **不诚实行为**：不得隐瞒问题或错误

## 验证要求
1. **单元测试**：每个功能必须有对应的单元测试
2. **集成测试**：必须进行集成测试
3. **实际运行**：必须实际运行验证功能
4. **用户验收**：重要功能需要用户验收

## 违规后果
1. **立即停止**：发现违规立即停止所有工作
2. **重新开始**：从正确的第一步重新开始
3. **记录问题**：记录违规原因和改进措施
4. **汇报机制**：及时汇报工作状态和问题