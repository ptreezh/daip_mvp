# iFlow 项目配置文档

## 项目概述

DAIP-LIVE (Dynamic AI-driven Project-execution LIVE system) 是一个运行在本地个人电脑上的智能协作与执行平台。它通过多AI角色协作、结构化辩论和本地知识库管理，为单个用户提供强大的决策和分析支持。

本系统**以本地大语言模型为核心，云端模型为辅助**，所有核心功能均可在离线环境下运行，确保了用户的隐私和数据安全。

## 系统架构

DAIP-LIVE (单机版) 采用**模块化单体应用架构**。整个系统作为一个独立的本地Python进程运行，其核心是一个**动态的、分层的Agent执行引擎**，而非一个静态的工作流系统。此架构支持用户通过**异步消息队列**对AI的思考和执行过程进行**实时驾驭 (Real-time Steering)**，代表了下一代AI助手的交互模式。

### 核心设计原则

- **动态与自适应**: AI的行为由其自身的**规划（Todo系统）**和对环境的感知驱动，而非完全依赖预设的静态流程。
- **实时交互**: 用户是"驾驶员"，而非"乘客"。用户可以在AI执行任务的任何时刻进行干预、提问和修正。
- **隔离与并发**: 通过**子Agent (SubAgent)** 机制，将复杂任务分解给隔离的、并行的执行单元，提升效率和稳定性。
- **安全与可控**: 通过**多层权限**和**前置条件检查**，确保AI工具的使用既强大又安全。

## 项目最高准则

### 1. 工作日志记录原则
- **所有新工作必须记录到工作日志**：每天新建一个 `worklog+日期.md` 文件在 `log/` 目录下
- **工作日志必须包含**：日期、任务描述、实施条件检查结果、置信度评估、实施结果

### 2. 实施条件检查原则
- **文档先行、契约先行**：必须有符合TDD BMAD kiro's spec的规范文档
- **需求明晰性检查**：需求必须与项目最高需求原则对齐
- **设计原则检查**：必须符合KISS YAGNI SOLID原则
- **置信度评估**：置信度必须高于0.96，否则进入研究模式

### 3. TDD BMAD kiro's spec规范
- **TDD驱动**：测试先行，红-绿-重构循环
- **BMAD方法**：行为驱动开发，明确业务价值
- **KIRO规范**：知识、接口、角色、操作的清晰定义
- **任务清单**：每个任务必须有明晰的上下文和验收标准

### 4. 文档修改原则
- **影响分析**：文档修改必须检查对其他模块的影响
- **先修订后实施**：必须先修订受影响的文档，再实施代码修改
- **版本控制**：所有文档修改必须有版本记录和影响说明

### 5. 研究模式触发条件
- **置信度<0.96**：需求不明确或实施条件不充分
- **文档缺失**：缺少必要的规范文档
- **需求冲突**：与现有需求或架构原则冲突
- **技术风险**：存在未解决的技术难点

### 6. 实施流程
1. **需求分析**：明确需求并评估置信度
2. **文档编写**：编写符合规范的文档
3. **条件检查**：核验实施条件是否满足
4. **任务分解**：基于TDD创建任务清单
5. **迭代实施**：逐个任务基于TDD实施
6. **验证测试**：确保所有测试通过
7. **文档更新**：更新相关文档和日志

## 系统架构图 (动态Agent模型)

```
┌──────────────────────────────────────────────────────────────────┐
│                        DAIP-LIVE Application                     │
│ ┌──────────────────────────────────────────────────────────────┐ │
│ │                    交互层 (Interface Layer)                    │ │
│ │   ┌──────────┐   ┌───────────┐   ┌─────────────────────────┐   │ │
│ │   │ CLI/TUI  │──▶│  异步消息 │◀──│      图形用户界面 (GUI)   │   │ │
│ │   └──────────┘   │   队列    │   └─────────────────────────┘   │ │
│ │                  └─────┬─────┘                               │ │
│ └────────────────────────┼─────────────────────────────────────┘ │
│                          │ (实时指令/Steering)                     │
│ ┌────────────────────────▼─────────────────────────────────────┐ │
│ │                  核心逻辑层 (Core Logic Layer)                 │ │
│ │   ┌────────────────────────────────────────────────────────┐ │ │
│ │   │              主Agent执行引擎 (Main Agent Engine)         │ │ │
│ │   │ ┌───────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │ │ │
│ │   │ │ Todo List │═▶│   思考   │═▶│   工具   │═▶│   响应   │ │ │ │
│ │   │ │  (规划)   │  │ (Reason) │  │  (Tool)  │  │(Respond) │ │ │ │
│ │   │ └───────────┘  └─────┬────┘  └────┬─────┘  └──────────┘ │ │ │
│ │   └──────────────────────┼───────────┼────────────────────┘ │ │
│ │                          │           │ (调用)                 │ │
│ │   (调用) ┌───────────────┘           └─────────────────┐      │ │
│ │          ▼                                             ▼      │ │
│ │   ┌─────────────┐                             ┌─────────────┐ │ │
│ │   │  辩论系统   │                             │  角色管理器 │ │ │
│ │   │(Debate System)│                             │(Role Manager) │ │ │
│ │   └─────────────┘                             └─────────────┘ │ │
│ └──────────────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────────────┐ │
│ │                  核心服务层 (Core Service Layer)               │ │
│ │      (内存服务, 知识管理器, 模型提供者, 数据持久化层)        │ │
│ └──────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

## 技术栈

- **语言**: Python >=3.9, <3.12
- **包管理**: Poetry
- **CLI框架**: Typer
- **TUI框架**: Textual
- **GUI框架**: Streamlit
- **数据验证**: Pydantic
- **数据持久化**: SQLite
- **知识管理**: FAISS, Langchain
- **开发工具**: pytest, mypy, ruff, pre-commit

## 开发实践

- **使用 Poetry 进行依赖管理**
- **使用 ruff 进行代码格式化和 linting**
- **使用 mypy 进行类型检查**
- **使用 pytest 进行测试**
- **使用 pre-commit 钩子确保代码质量**
- **遵循 BMAD方法和Kiro规范**
- **采用 TDD驱动开发原则**
- **遵循 SOLID设计原则**

## 项目最高准则（新增）

### 📋 工作日志记录原则
- **所有新工作必须记录到工作日志**：每天新建一个 `worklog+日期.md` 文件在 `log/` 目录下
- **工作日志必须包含**：日期、任务描述、实施条件检查结果、置信度评估、实施结果

### 🔍 实施条件检查原则
- **文档先行、契约先行**：必须有符合TDD BMAD kiro's spec的规范文档
- **需求明晰性检查**：需求必须与项目最高需求原则对齐
- **设计原则检查**：必须符合KISS YAGNI SOLID原则
- **置信度评估**：置信度必须高于0.96，否则进入研究模式

### 🧪 TDD BMAD kiro's spec规范
- **TDD驱动**：测试先行，红-绿-重构循环
- **BMAD方法**：行为驱动开发，明确业务价值
- **KIRO规范**：知识、接口、角色、操作的清晰定义
- **任务清单**：每个任务必须有明晰的上下文和验收标准

### 📖 文档修改原则
- **影响分析**：文档修改必须检查对其他模块的影响
- **先修订后实施**：必须先修订受影响的文档，再实施代码修改
- **版本控制**：所有文档修改必须有版本记录和影响说明

### 🔬 研究模式触发条件
- **置信度<0.96**：需求不明确或实施条件不充分
- **文档缺失**：缺少必要的规范文档
- **需求冲突**：与现有需求或架构原则冲突
- **技术风险**：存在未解决的技术难点

### 🔄 实施流程
1. **需求分析**：明确需求并评估置信度
2. **文档编写**：编写符合规范的文档
3. **条件检查**：核验实施条件是否满足
4. **任务分解**：基于TDD创建任务清单
5. **迭代实施**：逐个任务基于TDD实施
6. **验证测试**：确保所有测试通过
7. **文档更新**：更新相关文档和日志

## 工作日志规范

### 每日工作日志格式
```markdown
# 工作日志 - YYYY-MM-DD

## 今日任务
- [ ] 任务1：描述
- [ ] 任务2：描述

## 实施条件检查
- [ ] 需求明晰性：置信度评估
- [ ] 文档完整性：符合TDD BMAD kiro's spec
- [ ] 设计原则：KISS YAGNI SOLID检查
- [ ] 影响分析：相关模块文档检查

## 实施结果
- 任务1：结果描述
- 任务2：结果描述

## 置信度评估
- 整体置信度：0.XX
- 需要进一步研究的点：
  - 点1：原因
  - 点2：原因

## 明日计划
- 计划1
- 计划2
```

## 质量保证

- **测试覆盖率**：要求测试覆盖率达到90%以上
- **持续集成**：通过自动化测试确保代码质量
- **代码审查**：所有代码变更必须经过审查
- **文档同步**：代码变更必须同步更新相关文档

## 国际化支持

- **字符串外部化**：所有面向用户的字符串都不能硬编码在代码中
- **语言资源文件**：支持中文（`zh_CN`）和英文（`en`）两种语言
- **语言切换**：系统应能自动检测操作系统的语言设置

## 安全与权限控制

- **工具权限控制**：工具支持allow/deny/ask三种权限模式
- **路径遍历防护**：文件操作具有安全检查机制
- **API密钥管理**：API密钥通过加密方式安全存储
- **输入验证**：对所有用户输入进行严格验证