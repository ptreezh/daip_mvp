# iFlow 项目配置文档

## 项目概述

DAIP-LIVE (Dynamic AI-driven Project-execution LIVE system) 是一个运行在本地个人电脑上的智能协作与执行平台。它通过多AI角色协作、结构化辩论和本地知识库管理，为单个用户提供强大的决策和分析支持。

本系统**以本地大语言模型为核心，云端模型为辅助**，所有核心功能均可在离线环境下运行，确保了用户的隐私和数据安全。

**最新特性**：
- **自然语言意图识别**：支持用户用自然语言表达意图，系统自动识别并调用相应工具
- **智能论文检索**：集成arXiv论文搜索和下载功能
- **增强的TUI交互**：支持链接点击跳转、内容复制等交互功能
- **Wiki协同编辑**：内置Wiki系统支持知识管理和协同编辑
- **自动上下文压缩**：智能管理对话历史，优化内存使用

## 系统架构

DAIP-LIVE (单机版) 采用**模块化单体应用架构**。整个系统作为一个独立的本地Python进程运行，其核心是一个**动态的、分层的Agent执行引擎**，而非一个静态的工作流系统。此架构支持用户通过**异步消息队列**对AI的思考和执行过程进行**实时驾驭 (Real-time Steering)**，代表了下一代AI助手的交互模式。

### 核心设计原则

- **动态与自适应**: AI的行为由其自身的**规划（Todo系统）**和对环境的感知驱动，而非完全依赖预设的静态流程。
- **实时交互**: 用户是"驾驶员"，而非"乘客"。用户可以在AI执行任务的任何时刻进行干预、提问和修正。
- **隔离与并发**: 通过**子Agent (SubAgent)** 机制，将复杂任务分解给隔离的、并行的执行单元，提升效率和稳定性。
- **安全与可控**: 通过**多层权限**和**前置条件检查**，确保AI工具的使用既强大又安全。

### 系统架构图 (动态Agent模型)

```
┌──────────────────────────────────────────────────────────────────┐
│                        DAIP-LIVE Application                     │
│ ┌──────────────────────────────────────────────────────────────┐ │
│ │                    交互层 (Interface Layer)                    │ │
│ │   ┌──────────┐   ┌───────────┐   ┌─────────────────────────┐   │ │
│ │   │ CLI/TUI  │──▶│  异步消息 │◀──│      图形用户界面 (GUI)   │   │ │
│ │   └──────────┘   │   队列    │   └─────────────────────────┘   │ │
│ │                  └─────┬─────┘                               │ │
│ └────────────────────────┼─────────────────────────────────────┘ │
│                          │ (实时指令/Steering)                     │
│ ┌────────────────────────▼─────────────────────────────────────┐ │
│ │                  核心逻辑层 (Core Logic Layer)                 │ │
│ │   ┌────────────────────────────────────────────────────────┐ │ │
│ │   │              主Agent执行引擎 (Main Agent Engine)         │ │ │
│ │   │ ┌───────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │ │ │
│ │   │ │ Todo List │═▶│   思考   │═▶│   工具   │═▶│   响应   │ │ │ │
│ │   │ │  (规划)   │  │ (Reason) │  │  (Tool)  │  │(Respond) │ │ │ │
│ │   │ └───────────┘  └─────┬────┘  └────┬─────┘  └──────────┘ │ │ │
│ │   └──────────────────────┼───────────┼────────────────────┘ │ │
│ │                          │           │ (调用)                 │ │
│ │   (调用) ┌───────────────┘           └─────────────────┐      │ │
│ │          ▼                                             ▼      │ │
│ │   ┌─────────────┐                             ┌─────────────┐ │ │
│ │   │  辩论系统   │                             │  角色管理器 │ │ │
│ │   │(Debate System)│                             │(Role Manager) │ │ │
│ │   └─────────────┘                             └─────────────┘ │ │
│ └──────────────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────────────┐ │
│ │                  核心服务层 (Core Service Layer)               │ │
│ │      (内存服务, 知识管理器, 模型提供者, 数据持久化层)        │ │
│ └──────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

## 核心模块说明

### 交互层 (Interface Layer)
- **CLI/TUI/GUI**: 用户的操作入口。
- **异步消息队列 (Async Message Queue)**: 这是实现实时驾驭的关键。用户的任何输入都会被立即放入此队列。主Agent执行引擎在循环的每个关键节点都会检查此队列，以获取用户的最新指令，从而实现动态调整。

### 核心逻辑层 (Core Logic Layer)
- **主Agent执行引擎**: 取代了旧的"工作流引擎"。它不是执行一个固定的流程，而是围绕一个**"思考-行动"循环**来工作，其行为由一个内置的`Todo`列表（任务规划）来指导。它可以调用其他逻辑模块或服务层的工具。
- **子Agent派生能力**: 引擎中的`Task`工具可以根据需要，实例化一个或多个隔离的**子Agent**来并行处理复杂任务。
- **辩论系统 / 角色管理器**: 作为被主Agent引擎调用的高级逻辑模块。

### 核心服务层 (Core Service Layer)
- **数据持久化服务 (P1)**: 负责管理结构化数据的生命周期，包括数据库的初始化、表的创建，以及对核心对象的增、删、改、查操作。
- **知识管理服务 (P2)**: 负责实现完整的RAG（检索增强生成）流程中的"检索"部分，将非结构化文档转化为可检索的知识。
- **模型提供者服务 (P3)**: 作为系统与大语言模型的桥梁，通过工厂模式和统一接口实现对不同LLM后端的可插拔支持。
- **角色与工具核心 (P4)**: 作为AI"大脑"与"手"的关键枢纽，负责解析AI角色定义并提供安全的工具调用框架。
- **统一人类助手 (P8)**: 作为系统的智能协调中心，负责端到端的多角色工作流编排、用户记忆管理、提示词优化和Token优化。

## 项目文件结构

```
refactdoc/
├── src/
│   └── daip_live/
│       ├── agent_engine/              # Agent执行引擎
│       │   ├── executor.py            # 主执行器
│       │   ├── chat_executor.py       # 聊天执行器
│       │   ├── intent_recognizer.py   # 自然语言意图识别器
│       │   ├── session_manager.py     # 会话管理器
│       │   ├── state_manager.py       # 状态管理器
│       │   ├── step_executor.py       # 步骤执行器
│       │   └── workflow_executor.py   # 工作流执行器
│       ├── basic_tools/               # 基础工具集
│       │   ├── core.py                # 核心工具实现
│       │   └── __init__.py            # 工具注册和接口
│       ├── core/                      # 核心模块
│       │   ├── models.py              # 数据模型定义
│       │   ├── interfaces.py          # 接口定义
│       │   ├── exceptions.py          # 异常定义
│       │   └── config.py              # 配置管理
│       ├── doc/                       # 文档相关工具
│       ├── knowledge/                 # 知识管理服务
│       │   ├── manager.py             # 知识管理器
│       │   └── index.faiss            # FAISS向量索引
│       ├── memory/                    # 内存服务
│       │   ├── service.py             # 内存服务实现
│       │   └── session_manager.py     # 会话管理器
│       ├── model_provider/            # 模型提供者服务
│       ├── p4_role_manager_tools/     # 角色管理器与工具
│       ├── p7_gui/                    # 图形用户界面
│       ├── p8_debate_system/          # 辩论系统
│       ├── permission/                # 权限管理
│       ├── persistence/               # 数据持久化
│       ├── scaffolding/               # 项目脚手架
│       ├── security/                  # 安全模块
│       ├── vcs/                       # 版本控制
│       ├── wiki/                      # Wiki系统
│       │   ├── manager.py             # Wiki管理器
│       │   ├── models.py              # Wiki数据模型
│       │   ├── tui.py                 # Wiki TUI界面
│       │   └── knowledge_integration.py # 知识集成
│       ├── workflow/                  # 工作流管理
│       ├── cli.py                     # CLI入口
│       ├── tui.py                     # TUI界面
│       └── ...                        # 其他核心文件
├── roles/                             # AI角色定义文件
├── docs/                              # 文档目录
├── prompts/                           # 提示词模板
├── knowledge/                         # 知识库索引
├── wiki/                              # Wiki页面存储
├── log/                               # 日志目录
├── IFLOW.md                           # iFlow配置文档
├── config.yaml                        # 配置文件
├── pyproject.toml                     # 项目配置文件
└── ...                                # 其他项目文件
```

## 技术栈

- **语言**: Python >=3.9, <3.13
- **包管理**: Poetry
- **CLI框架**: Typer
- **TUI框架**: Textual
- **GUI框架**: Streamlit
- **数据验证**: Pydantic
- **数据持久化**: SQLite
- **知识管理**: FAISS-CPU, Langchain
- **模型提供者**: LiteLLM, Ollama
- **文档处理**: python-docx, PyYAML
- **学术研究**: arxiv, scholarly
- **Web框架**: FastAPI, Uvicorn
- **依赖注入**: dependency-injector
- **开发工具**: pytest, mypy, ruff, pre-commit

### 核心依赖详解

**AI与模型**:
- `litellm ^1.76.1`: 统一的多模型接口，支持本地和云端LLM
- `faiss-cpu ^1.8.0`: 向量相似度搜索，用于知识检索
- `langchain ^0.2.5`: LLM应用开发框架

**文档与知识**:
- `arxiv ^2.1.0`: arXiv论文搜索和下载
- `python-docx ^1.1.2`: Word文档处理
- `scholarly ^1.7.11`: 学术文献搜索

**用户界面**:
- `textual ^0.66.0`: 现代终端用户界面框架
- `streamlit ^1.35.0`: Web界面框架
- `rich ^14.1.0`: 终端富文本显示

## 开发实践

- 使用 Poetry 进行依赖管理
- 使用 ruff 进行代码格式化和 linting
- 使用 mypy 进行类型检查
- 使用 pytest 进行测试
- 使用 pre-commit 钩子确保代码质量

## CLI 命令结构

系统提供统一的入口命令`daip`，支持以下子命令：

### 核心命令
- **`daip agent run ["<goal>"]`**: 启动智能代理，支持自然语言意图识别
- **`daip tui`**: 启动文本用户界面（推荐使用）
- **`daip gui`**: 启动图形用户界面

### 知识管理
- **`daip knowledge sync`**: 同步知识库，索引文档
- **`daip knowledge search "<query>"`**: 搜索知识库内容

### 文档处理
- **`daip doc search <query>`**: 搜索学术论文
- **`daip doc download <paper_id>`**: 下载指定论文

### 角色与辩论
- **`daip role list`**: 列出所有可用角色
- **`daip role view <role_name>`**: 查看角色详细信息
- **`daip debate start <topic>`**: 启动智能辩论

### 会话管理
- **`daip session list`**: 列出历史会话
- **`daip session view <session_id>`**: 查看会话详情
- **`daip session clear`**: 清理当前会话上下文

### 项目管理
- **`daip project scaffold <description>`**: 初始化新项目
- **`daip wiki create <title>`**: 创建Wiki页面
- **`daip wiki list`**: 列出Wiki页面

### 权限管理
- **`daip permission list`**: 查看权限设置
- **`daip permission grant <tool> <level>`**: 授予权限
- **`daip permission revoke <tool>`**: 撤销权限

## 自然语言交互

系统支持自然语言意图识别，用户可以直接用自然语言表达需求：

### 论文检索示例
```
用户: "搜索机器学习相关的论文"
系统: 自动识别为论文搜索意图，调用arXiv搜索API

用户: "下载论文2301.00001"
系统: 自动识别为论文下载意图，调用下载工具
```

### 辩论功能示例
```
用户: "我们来辩论一下人工智能的未来"
系统: 自动识别为辩论意图，启动多角色辩论
```

### Wiki管理示例
```
用户: "创建一个关于深度学习的wiki页面"
系统: 自动识别为Wiki创建意图，启动Wiki编辑器
```

### 上下文管理示例
```
用户: "压缩一下对话历史"
系统: 自动识别为压缩意图，清理历史记录
```

## TUI 核心交互界面 ("Gemini模式")

当用户运行`daip tui`或`daip agent run`等交互式命令时，系统启动全屏TUI应用，严格实现"Gemini模式"交互需求。

### 界面布局
- **主聊天窗口**: 可滚动的对话历史区域
- **状态栏**: 实时显示系统状态
  - 当前模型（如 `ollama/qwen:0.5b`）
  - Token使用率和颜色指示
  - 系统状态（思考、执行工具等）
  - 请求计数和平均延迟
- **输入框**: 底部输入区域，支持异步消息队列

### 内容渲染要求
1. **Agent思考/计划**: 特殊样式显示（斜体、特定颜色）
2. **工具调用**: 带边框高亮区块，显示工具名和参数
3. **工具输出**: 带成功/失败标识的区块，支持语法高亮和滚动
4. **LLM回复**: 流式"打字机"效果显示
5. **Markdown渲染**: 正确渲染列表、粗体、斜体和代码块

### 增强交互功能
- **链接点击跳转**: 自动识别HTTP/HTTPS链接和邮件地址，点击后启动对应应用
- **内容复制**: 支持Ctrl+A全选和Ctrl+C复制输出内容
- **焦点模式**: 支持输入/输出模式切换（Shift+Tab）
- **命令自动补全**: 支持/命令的Tab补全和提示

### 键盘快捷键
- `Shift+Tab`: 切换输入/输出焦点
- `Ctrl+A`: 全选输出内容
- `Ctrl+C`: 复制输出内容
- `Escape`: 退出输出模式
- `Ctrl+E`: 退出应用（双击确认）

### 错误处理与容错
- 状态栏更新失败时的降级处理
- 工具执行错误的友好提示
- 网络和服务异常的自动重试机制

## GUI 核心交互界面 ("Gemini模式")

GUI界面同样需要严格遵循"Gemini模式"的交互需求：

**页面布局:**
- **主会话页面**: 核心的对话和任务执行界面，包含可滚动的对话历史记录区域。
- **知识库管理页面**: 用于管理本地知识源，显示已索引文件的列表、状态和统计信息。
- **设置页面**: 用于配置系统和管理密钥，包括模型提供者切换、API密钥管理、工具权限管理等。

**信息展示要求:**
1.  **显式的意图与规划**: 在执行任何复杂操作或调用工具前，系统应首先清晰地展示其"思考"或"计划"。
2.  **透明的工具调用**: 当系统使用工具时，必须在界面上以一个独立的、高亮的区块展示完整的工具调用信息。
3.  **明确的工具输出**: 工具执行后的返回结果，必须在一个独立的、结构化的区块中展示，使用颜色或图标明确标识成功/失败状态。
4.  **流式响应**: 所有来自LLM的最终文字回答，都必须以流式"打字机"效果逐字显示。
5.  **Markdown渲染**: 界面必须能正确解析并渲染LLM回答中的Markdown语法。

## 新增核心功能

### 自然语言意图识别系统
系统集成了智能意图识别器，支持以下意图类型：

- **论文检索意图**: 自动识别"搜索论文"、"查找论文"等关键词
- **论文下载意图**: 自动识别arXiv ID并转换为下载命令  
- **辩论功能意图**: 自动识别辩论话题和参与角色
- **Wiki创建意图**: 自动识别Wiki页面创建需求
- **上下文压缩意图**: 自动识别压缩需求，优化内存使用
- **项目初始化意图**: 自动识别项目初始化需求

### 学术研究集成
- **arXiv论文搜索**: 支持关键词搜索，返回相关论文信息
- **论文下载**: 支持通过arXiv ID下载PDF论文
- **参考文献管理**: 自动提取和管理论文引用信息

### Wiki知识管理系统
- **页面创建与编辑**: 支持Markdown格式的Wiki页面
- **标签管理**: 支持页面标签分类和组织
- **全文搜索**: 基于FAISS的向量搜索功能
- **版本控制**: 支持页面历史版本管理

### 智能上下文管理
- **自动压缩**: 当Token使用率达到80%时自动触发
- **智能摘要**: 使用LLM生成对话历史摘要
- **分层存储**: 短期、中期、长期记忆分层管理

## 系统优化与修复

### 已修复的问题
1. **Session类型错误**: 修复了auto_compression类型匹配问题
2. **模型切换状态栏**: 修复了模型切换后状态栏不更新的问题
3. **内容复制功能**: 移除了复制功能的焦点模式限制
4. **Wiki页面创建**: 修复了缺少_wiki_manager的初始化问题
5. **FAISS索引错误**: 修复了IndexFlatL2不支持add_with_ids的问题

### 性能优化
- **异步处理**: 所有I/O操作均采用异步处理
- **缓存机制**: 实现了模型响应和搜索结果缓存
- **内存管理**: 优化了大型文档的内存使用

## 国际化支持

- **字符串外部化**: 所有面向用户的字符串都不能硬编码在代码中，必须使用标准的i18n框架进行管理。
- **语言资源文件**: 支持中文（`zh_CN`）和英文（`en`）两种语言，语言资源文件存放在指定的`locales/`目录中。
- **语言切换**: 系统能自动检测操作系统的语言设置，并将其作为默认语言，用户也能在设置界面或通过`config.yaml`文件手动切换语言。

## 部署与运行

### 环境要求
- Python 3.9-3.12
- 至少8GB内存（推荐16GB）
- 支持向量计算的CPU（可选GPU支持）

### 快速启动
```bash
# 安装依赖
poetry install

# 启动TUI界面
poetry run daip tui

# 启动GUI界面
poetry run daip gui

# 命令行模式
poetry run daip agent run "你的目标"
```

### 配置说明
主要配置文件`config.yaml`：
```yaml
database:
  path: "daip_live.db"

llm_provider:
  default_model: "ollama/llama3:8b"
  embedding_model: "mock-embedding"

knowledge_base:
  directory: "knowledge"

role_manager:
  roles_dir: "roles"
```