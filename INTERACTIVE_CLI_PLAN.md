# DAIP-LIVE TDD-驱动的交互式CLI实施计划

## 1. 核心原则
- **测试驱动开发 (TDD)**: 所有功能的开发都必须由测试用例驱动。严格遵循 Red-Green-Refactor 循环。
- **API字典中心化**: `GLOBAL_API_DICTIONARY.md` 是后端接口的唯一事实来源。所有CLI功能设计和测试都必须严格参照此文档。
- **严格的变更控制**: 任何对API字典的新增或修改都必须经过正式的请求-审批流程。

## 2. 状态跟踪
我将在此文件中维护每个阶段的执行状态。在完成一个阶段后，我会向您报告，并在获得您的确认后更新此处的状态，然后才能进入下一阶段。

## 3. 分阶段执行计划

### **阶段 1: 全局API发现、分析与存档**
- **目标**: 扫描并验证所有相关的后端API，识别其真实实现状态，并创建一份权威的全局API字典。
- **活动**:
    1.  对 `src` 目录进行系统性扫描，定位所有相关服务。
    2.  审查每个API方法的实现逻辑，将其分类为 `完整`, `Mock`, `桩` 或 `待简化`。
    3.  创建 `documentation/GLOBAL_API_DICTIONARY.md` 文件。
    4.  存档API字典，作为后续所有工作的基准。
- **状态**: `[x] 已完成`

### **阶段 2: TDD驱动的文档重构**
- **目标**: 将所有前期生成的模块文档（需求、设计、任务）重构为TDD模式。
- **活动**:
    1.  **重构需求**: 增加“验收测试用例”章节，并关联到API字典。
    2.  **重构设计**: 增加“测试策略”章节，确保组件交互严格引用API字典。
    3.  **重构任务**: 将所有任务列表重写为 Red-Green-Refactor (编写失败测试 -> 编写实现 -> 重构) 的格式。
- **状态**: `[x] 已完成`

### **阶段 3: TDD驱动的实现**
- **目标**: 根据重构后的任务列表，以TDD方式完成CLI的编码。
- **活动**:
    1.  严格按照新的任务列表，从编写失败的测试开始。
    2.  编写最少的代码让测试通过。
    3.  在测试保护下进行代码重构。
- **状态**: `[ ] 未开始`

## 4. API变更控制流程
如果在开发中发现必须新增API，我将暂停任务并向您提交一份**API新增请求**，包含：
- **API提议**: 详细的方法签名和描述。
- **必要性理由**: 解释为何现有API无法满足需求。
- **影响分析**: 对现有代码的潜在影响。
在获得您的明确批准前，绝不实现或使用新API。