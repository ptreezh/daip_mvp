# DAIP-LIVE V0.1 到 V0.2 升级指南

## 📋 升级概述

本指南帮助您从DAIP-LIVE V0.1平滑升级到V0.2.0"场景增强版"。V0.2版本完全向后兼容V0.1，升级过程安全且简单。

**升级类型**: 功能增强型升级  
**兼容性**: 100% 向后兼容  
**升级时间**: 约5-10分钟  
**停机时间**: 无需停机  

## 🎯 V0.2新增功能预览

### 🌟 核心新功能
- **📚 学术研究场景**: 深度研究分析和万字报告生成
- **👨‍💼 专家咨询场景**: 智能专家匹配和决策建议
- **😊 轻松讨论场景**: 自然对话和社交互动
- **🤖 智能场景推荐**: 自动识别最适合的协作模式
- **🌐 完整Web界面**: 现代化响应式Web体验

### 📈 性能改进
- 场景切换优化
- LLM调用错误处理增强
- Web界面响应性能提升
- 并发处理能力增强

## 🔄 升级步骤

### 步骤1: 环境准备

#### 1.1 检查当前版本
```bash
cd /path/to/daip-live
git branch
git log --oneline -5
```

#### 1.2 备份当前配置（可选）
```bash
# 备份配置文件
cp config.yaml config.yaml.backup
cp -r data/ data_backup/
cp -r roles/ roles_backup/
```

#### 1.3 检查依赖环境
```bash
# 检查Python版本 (需要3.8+)
python --version

# 检查虚拟环境
which python
```

### 步骤2: 代码更新

#### 2.1 拉取V0.2代码
```bash
# 获取最新代码
git fetch origin

# 切换到V0.2版本
git checkout main
git pull origin main

# 或者使用tag方式
git checkout v0.2.0
```

#### 2.2 安装依赖
```bash
# 使用Poetry (推荐)
poetry install

# 或使用pip
pip install -e .
```

### 步骤3: 配置验证

#### 3.1 检查配置兼容性
V0.2完全兼容V0.1配置，无需修改`config.yaml`。

#### 3.2 验证LLM配置
```bash
# 检查Ollama服务 (如果使用)
ollama list
ollama serve

# 测试LLM连接
python -c "
from src.core_services.integrated_llm_manager import IntegratedLLMManager
llm = IntegratedLLMManager()
print('✅ LLM集成正常')
"
```

### 步骤4: 功能验证

#### 4.1 启动V0.2服务
```bash
# 启动Web界面 (新功能)
python integrated_web_demo.py

# 访问Web界面
# http://localhost:8000
```

#### 4.2 测试V0.1功能 (兼容性验证)
```bash
# 测试原有CLI功能
daip-cli status
daip-cli roles

# 测试原有API端点
curl http://localhost:8000/health
```

#### 4.3 测试V0.2新功能
```bash
# 测试智能场景推荐
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"user_input": "AI在教育中的应用研究"}'

# 测试场景API
curl -X POST http://localhost:8000/api/scenario \
  -H "Content-Type: application/json" \
  -d '{"topic": "微服务架构", "scenario_type": "expert_consultation"}'
```

## 🛠️ 配置调整 (可选)

### 新增配置选项

V0.2引入了一些可选的新配置，您可以根据需要添加到`config.yaml`:

```yaml
# V0.2新增场景配置 (可选)
scenarios:
  academic_research:
    max_word_count: 15000
    research_depth: "deep"
    citation_style: "academic"
  
  expert_consultation:
    max_experts: 7
    consultation_timeout: 300
    decision_framework: "comprehensive"
  
  casual_discussion:
    max_participants: 4
    social_elements: true
    humor_level: 0.7

# Web界面配置 (可选)
web_interface:
  theme: "modern"
  real_time_updates: true
  performance_monitoring: true
```

### LLM配置优化

如果您想优化V0.2的LLM性能:

```yaml
# 在config.yaml中添加
llm_config:
  scenario_recommendation:
    model: "llama3:instruct"
    temperature: 0.7
    max_tokens: 2000
  
  academic_research:
    model: "llama3:instruct"
    temperature: 0.3
    max_tokens: 8000
```

## 🔍 升级验证清单

### ✅ 基础功能验证
- [ ] 服务正常启动
- [ ] V0.1 CLI命令正常工作
- [ ] V0.1 API端点正常响应
- [ ] 配置文件正确加载
- [ ] LLM服务连接正常

### ✅ V0.2新功能验证
- [ ] Web界面可以正常访问 (http://localhost:8000)
- [ ] 智能场景推荐正常工作
- [ ] 学术研究场景生成报告
- [ ] 专家咨询场景提供建议
- [ ] 轻松讨论场景自然对话
- [ ] 场景间切换流畅

### ✅ 性能验证
- [ ] 响应时间在预期范围内 (<3秒)
- [ ] 内存使用正常
- [ ] 并发处理稳定
- [ ] 错误处理正确

## 🚨 故障排除

### 常见问题及解决方案

#### 问题1: Web界面无法访问
```bash
# 检查端口占用
netstat -ano | findstr :8000

# 检查防火墙设置
# Windows: 允许Python通过防火墙
# Linux: sudo ufw allow 8000
```

#### 问题2: LLM服务连接失败
```bash
# 重启Ollama服务
ollama serve

# 检查模型是否下载
ollama list
ollama pull llama3:instruct
```

#### 问题3: 新场景功能不可用
```bash
# 检查依赖安装
pip install -e .

# 验证模块导入
python -c "
from src.scenarios.scenario_manager import ScenarioManager
print('✅ 场景管理器导入成功')
"
```

#### 问题4: 性能下降
```bash
# 检查系统资源
htop  # Linux
tasklist  # Windows

# 优化配置
# 在config.yaml中调整LLM参数
```

## 🔄 回滚方案

如果升级过程中遇到问题，可以快速回滚到V0.1:

### 快速回滚
```bash
# 回滚到V0.1版本
git checkout v0.1.0

# 恢复配置 (如果修改过)
cp config.yaml.backup config.yaml
cp -r data_backup/ data/

# 重启服务
python run.py
```

### 完整回滚
```bash
# 完全重置到V0.1状态
git reset --hard v0.1.0
git clean -fd

# 重新安装V0.1依赖
pip install -e .
```

## 📊 升级后的新功能使用

### Web界面使用
```bash
# 启动Web演示
python integrated_web_demo.py

# 浏览器访问
http://localhost:8000
```

### 新的CLI命令
```bash
# 智能场景聊天
daip-cli chat "输入任意内容"

# 指定场景处理
daip-cli scenario academic "研究主题"
daip-cli scenario expert "咨询问题"
daip-cli scenario casual "聊天话题"
```

### API集成示例
```python
# Python API使用
from src.scenarios.scenario_manager import ScenarioManager

# 创建场景管理器
manager = ScenarioManager()

# 智能推荐和处理
result = await manager.recommend_and_execute(
    user_input="AI在教育中的应用",
    user_id="user123"
)

# 指定场景处理
academic_result = await manager.execute_scenario(
    scenario_type="academic_research",
    input_data={"research_topic": "机器学习算法"},
    user_id="user123"
)
```

## 🎉 升级完成

恭喜！您已成功升级到DAIP-LIVE V0.2.0。现在您可以享受：

- 🌐 **完整Web体验**: 现代化的用户界面
- 🤖 **智能场景推荐**: AI自动选择最佳协作模式
- 📚 **学术研究能力**: 深度分析和报告生成
- 👨‍💼 **专家咨询服务**: 权威决策建议和分析
- 😊 **轻松讨论体验**: 自然流畅的对话互动

## 💡 下一步建议

1. **探索新功能**: 尝试不同的场景和用户故事
2. **性能调优**: 根据使用情况调整配置参数
3. **用户培训**: 向团队介绍V0.2的新功能
4. **反馈收集**: 收集用户体验反馈，为后续版本改进
5. **关注更新**: 订阅项目更新，及时获取新版本信息

## 📞 技术支持

如果升级过程中遇到任何问题：

- **文档**: 查看 [README.md](README.md)
- **问题反馈**: [GitHub Issues](https://github.com/your-org/daip-live/issues)
- **社区支持**: [项目社区](https://community.daip-live.org)

---

🎊 **欢迎进入DAIP-LIVE V0.2智能协作新时代！**