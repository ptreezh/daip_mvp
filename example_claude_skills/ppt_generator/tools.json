{
  "tools": [
    {
      "name": "generate_presentation",
      "description": "Generates a PowerPoint presentation from content",
      "input_schema": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "description": "Title of the presentation"
          },
          "content": {
            "type": "string",
            "description": "Content to include in the presentation, formatted with markdown-style headings"
          },
          "template": {
            "type": "string",
            "description": "Template to use for the presentation",
            "enum": ["default", "business", "academic", "technical"],
            "default": "default"
          }
        },
        "required": ["title", "content"]
      }
    }
  ]
}